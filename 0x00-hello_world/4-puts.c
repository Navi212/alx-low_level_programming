#include <stdio.h>

/**
 * main - Prints a string with Puts
 *
 * Return -  (Success);
 */

int main(void)
{
        puts("Programming is like building a multilingual puzzle \n");
        return (0);
}
