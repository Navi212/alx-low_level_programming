#include <stdio.h>

/**
 * main - Prints the exact phrase with proper grammer,
 * but the outcome is a piece of art,
 * Return : 0 if exited successfully but non zero value
 * if otherwise
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art, \n");
	return (0);
}
