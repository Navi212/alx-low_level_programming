#include <stdio.h>

/**
 * main - Prints exact phrase with proper grammer, but
 * the outcome is a piece of art,
 * Description : This is a program that prints the phrase
 * Return: 0 if exited properly, non zero if otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
