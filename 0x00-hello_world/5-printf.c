#include <stdio.h>

/**
 * main - prints phrase with proper grammer, but the 
 * outcome is a piece of art,
 * Return : 0 if exited properly but non zero otherwise
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
